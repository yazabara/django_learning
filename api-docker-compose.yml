version: "3.7"

networks:
  workout-portal-local:
    name: workout-portal-local

services:

  workout-portal-api-service:
    networks:
      -  workout-portal-local
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8080:8080"
    container_name: workout-portal-api-service

#  rics-api-db:
#    image: postgres:9.6.9
#    restart: always
#    container_name: rics-api-db
#    environment:
#    - POSTGRES_USER=rics
#    - POSTGRES_PASSWORD=ricsadmin
#    - POSTGRES_DB=rics
#    command: ["--autovacuum=off"]

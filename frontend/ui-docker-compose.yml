version: "3.7"

services:

  workout-portal-ui:
    build:
      context: .
    container_name: workout-portal-ui
    restart: always
    ports:
      - "4200:4200"
    command: ["start"]
    depends_on:
      - workout-portal-api-service
    links:
      - workout-portal-api-service
